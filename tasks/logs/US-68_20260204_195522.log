## Summary

I have successfully implemented **US-68: SetStockLevel Use Case**:

### Files Created:
1. **SetStockLevelInputPort.java** - Input port interface extending `UseCase<SetStockLevelCommand, SetStockLevelResult>`
2. **SetStockLevelCommand.java** - Command record with `productId` and `quantity` parameters
3. **SetStockLevelResult.java** - Result record with stockLevelId, productId, quantities, and created flag
4. **SetStockLevelUseCase.java** - Use case implementation that:
   - Looks up existing stock level by product ID
   - Creates new stock level if not exists
   - Updates existing stock level if exists
   - Publishes domain events via `DomainEventPublisher`

### File Modified:
- **StockLevel.java** - Added `setAvailableQuantity(int quantity)` method that:
  - Sets the available quantity to a specific value
  - Adjusts reserved quantity if it exceeds the new available quantity
  - Registers a `StockChanged` domain event

### Verification:
- Build passes: `./gradlew build` ✅
- Architecture tests have a pre-existing failure unrelated to this change (CheckoutValidationResult in domain layer)

<promise>STORY_COMPLETE</promise>
